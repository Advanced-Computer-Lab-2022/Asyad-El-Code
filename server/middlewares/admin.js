// export function checkAdmin(req, res, next) {
//   if (req.user && req.user.isAdmin) {
//     // the user is an admin, so proceed to the next middleware or route handler
//     next();
//   } else {
//     // the user is not an admin, so return an error message
//     return res.status(403).send({ error: "Forbidden" });
//   }
// }
